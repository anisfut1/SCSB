{
  "name": "fbi-auth-spike",
  "private": true,
  "version": "0.0.0",
  "description": "Spike technique local — FBI authentifié. Jamais déployé, jamais importé par l'application.",
  "type": "module",
  "scripts": {
    "probe": "tsx probe.ts",
    "check-session": "tsx probe.ts --check-session",
    "report": "tsx report.ts",
    "clean": "tsx clean.ts",
    "inspect-export": "tsx inspect-export.ts",
    "selftest": "tsx sanitize.selftest.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
